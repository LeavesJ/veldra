# Rust build output
target/
**/target/

# Per-service Cargo.lock (workspace root Cargo.lock is tracked)
services/*/Cargo.lock

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
.fuse_hidden*

# Env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Secrets and credentials — never commit
*.pem
*.key
*.p12
*.pfx
*.secret
*.crt
*.csr
id_rsa*
id_ed25519*

# Logs / traces
*.log
*.ndjson

# Runtime generated data
services/pool-verifier/data/
artifacts/

# Local-only config and policy overrides
config/local/
config/*.local.toml
config/policy.toml
services/pool-verifier/policy.toml

# Node
node_modules/
*.tsbuildinfo

# Scratch / temp files
*.patch
*.tmp
dist/
.tmp/

# QA screenshots and slide exports from pitchdeck work
qa-*.jpg
slide-*.jpg

# Pitchdeck unpack scratch directory
pitchdeck_unpack/

# Office lock files
.~lock.*
~$*

# SQLite runtime databases
*.db
*.db-shm
*.db-wal

# Private engineering docs (local only, all under docs/)
docs/DEVLOG.md
docs/lessons.md
docs/PRODUCTION_BLOCKERS.md
docs/BIZLOG.md
docs/EXECLOG.md
docs/TESTLOG.md
docs/DASHBOARD_SMOKE_TEST.md
docs/architecture-comparison.md
docs/MEETING_PREP.md
docs/DEEP_SCAN_*
docs/linkedin-playbook.md
docs/pitch-prep.md
docs/meeting-xu.md
docs/one-pager-xu.md
docs/outreach/
docs/SESSION_HANDOFF.md

# Private assets and design drafts (local only)
ReserveGrid-OS-Founders-Guide.md
ReserveGrid-OS-Founders-Guide.pdf
ReserveGrid-OS-Pitchdeck.pptx
ReserveGrid-OS-Pitchdeck-v2.pptx
ReserveGrid-OS-Pitchdeck-v2.pdf
ReserveGrid-OS-Pitchdeck-v3.pptx
ReserveGrid-OS-Security-Audit.pdf
ReserveGrid-OS-Internal-Reference.pdf
ReserveGrid-OS-One-Pager-EN-CN.pdf
Veldra-Strategic-Review-Consolidated.pdf
POSITIONING.md
generate_guide_pdf.py
security-audit-report.py
veldra-roadmap.jsx
rg-os-wireframes.html
/index.html
/account-index.html
/product-index.html
/shadow-vs-observe.html
/license-page.html
/section-redesign-preview.html
/script.js
/style.css
Cargo.lock.bak.old
pdf-export/
TEST_COVERAGE_AUDIT.md
/Dockerfile
/Dockerfile.gateway
/Dockerfile.rg-auth
/tauri.conf.json

# Website lives in LeavesJ/veldra-site (cloned locally as veldra-site/)
veldra-site/
veldra-site-v3/

# Release notes are tracked so the release-desktop.yml runner can read them
# on a fresh checkout. Local drafts named differently stay ignored if needed.

# Scan and audit scratch outputs
detailed_findings.md
resource_exhaustion_audit.md

# Claude Code config
CLAUDE.md
.claude/

# PowerPoint autosave noise
*.pptx

# Verifier scratch dir (verdict log, baseline JSON, runtime data) created by
# pool-verifier and the soak helper scripts. Not tracked.
data/

# Spotcheck log appended by `scripts/phase2-spot-check.sh | tee -a`. Mirrors
# the existing DEVLOG.md private-doc convention.
docs/DEVLOG.md.spotchecks

# Derived PDFs from tracked markdown briefs. Markdown is the single source
# of truth; regenerate via `pandoc <source>.md -o <source>.pdf` when needed.
# The -v1 file is a superseded render kept only for local comparison.
docs/site-redesign-content-additions.pdf
docs/site-redesign-content-additions-v1.pdf

# Private site working notes (AUDIT, DEV-STATUS, MOBILE-PLAN, SEO-DIAGNOSTIC).
# Part of the enumerated private-doc block, NOT derived output, so the
# private-docs pattern in scripts/gate.sh carries a matching `docs/site/`
# alternative per invariant 7.
docs/site/
